# Dev/test-only dependencies — not bundled into the Android app; pytest runs
# against the shared core, Chaquopy packages only requirements.txt.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
