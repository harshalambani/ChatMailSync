<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- allowBackup is true since v1.17.0. It was false because "back up an
         archiver's database" sounds like a privacy question, and the
         safe-looking answer was no. It is the wrong answer: what this app
         keeps is the record of what it has already mailed, and a device that
         loses it mails every chat a second time into a mailbox that cannot
         tell the copies apart. The two rules files say exactly what travels
         and what does not - the chat exports themselves never leave, and the
         mail password cannot be read off the device it was saved on. -->
    <application
        android:name=".ChatMailApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Chat Mail Sync"
        android:theme="@style/Theme.ChatMailSync"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ChatMailSync">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- WhatsApp "Export chat" share sheet: with-media exports are
                 .zip; without-media exports are plain .txt shared as
                 text/plain. Two filters (not two <data> in one filter) so
                 each mimeType is matched independently. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/zip" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.chatmailsync.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager's own SystemForegroundService declares no
             foregroundServiceType by default; SyncWorker.setForeground()
             requests dataSync, so it must be granted here explicitly or
             Android throws IllegalArgumentException at startForeground(). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
